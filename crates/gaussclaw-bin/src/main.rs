//! `gaussclaw-bin` — The shipping `gaussclaw` binary; wires every gaussclaw-* crate together.
//!
//! Phase 1 of the GaussClaw Hermes-to-Rust port. See
//! `GAUSSCLAW_ROADMAP.md` at the workspace root. This is the
//! scaffolding stub; the real wiring lands as each phase ships.

fn main() {
    println!("gaussclaw-bin: scaffolding — see GAUSSCLAW_ROADMAP.md");
}
